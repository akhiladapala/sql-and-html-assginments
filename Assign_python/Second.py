t = (10, 20, 30, 40, 50, 20, 60)

# 1.	Count occurrences of 20 using count(). 
# t.count(20)
print(t.count(20))
# 2.	Find index of 40 using index(). 
print(t.index(40))
# 3.	Convert tuple to list and append 70, then convert back. 
t1 = list(t)
t1.append(70)
t=tuple(t1)
print(t)
# 4.	Slice tuple to get elements from index 2 to 5. 

# 5.	Concatenate another tuple (80, 90). 
# 6.	Repeat tuple 2 times using *. 
# 7.	Check if 50 exists using in. 
# 8.	Unpack tuple into variables. 
# 9.	Try modifying element and explain error (immutability concept). 
# 10.	Create a nested tuple and access inner value. 